## Summary

-

## Validation

- [ ] `python scripts/validate_repo.py`
- [ ] Apply `ready-to-test` when the branch is ready for expensive container checks
- [ ] Runtime image build, when container inputs changed
- [ ] Export/parity benchmark, when model execution changed

## Deployment notes

- [ ] No model weights, generated IR, reference audio, or secrets are committed
- [ ] PyTorch rollback remains available
