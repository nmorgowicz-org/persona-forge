flask==3.1.3
gunicorn==26.0.0
soundfile==0.14.0
# qwen-tts==0.1.1 hard-pins transformers==4.57.3, so the Dockerfile installs it
# with --no-deps before this file to bypass that constraint (CVE-2026-1839).
# Its runtime dependencies that pip would normally resolve are listed below.
accelerate
einops
librosa
onnxruntime
sox
transformers==5.12.1

# OmniVoice (Persona Forge accent-design engine, docs/plans/PLAN_persona_forge_studio.md).
# Installed --no-deps in the Dockerfile since its declared deps (torch/torchaudio/
# transformers/accelerate/librosa/soundfile/numpy) already match the pins above exactly, and
# its unpinned extras (gradio/tensorboardx/webdataset) are unused by the inference path this
# service actually calls. pydub is the one genuinely new runtime dependency.
pydub
