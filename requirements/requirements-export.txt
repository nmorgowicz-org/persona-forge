# Export uses openvino.convert_model + nncf.compress_weights directly.
# Optimum Intel is intentionally excluded; see docs/archive/openvino/OPENVINO_IMPLEMENTATION.md
# ("Export library: openvino + nncf only") for the rationale.
-r requirements-openvino.txt
nncf==3.3.0
